# Dave Agent Persona
# Fourth test user on localhost (local development)

name: dave
display_name: "Dave Davis"
identity: "storm.garden.ice.sand"
role: "member"

# Node connection
node:
  host: "127.0.0.1"
  port: 3041
  name: "localhost"
  region: "Local"
  provider: "Local"

# Personality traits (for AI persona)
traits:
  - pragmatic
  - observant
  - patient

# Default test behaviors
behaviors:
  # Dave is typically a regular member, often used for sync tests
  creates_entities: false
  invites_members: false
  manages_permissions: false

# Vault credentials (for test scenarios)
vault:
  password: "test-pass-dave"

# Color for reports
color: "#8B5CF6"  # Purple
