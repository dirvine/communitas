# Charlie Agent Persona
# Third test user on saorsa-6 (Hetzner Helsinki)

name: charlie
display_name: "Charlie Chen"
identity: "wind.valley.fire.lake"
role: "member"

# Node connection (uses nginx proxy on port 80 due to Hetzner firewall)
node:
  host: "65.21.157.229"
  port: 80
  name: "saorsa-6"
  region: "Helsinki, FI"
  provider: "Hetzner"

# Personality traits (for AI persona)
traits:
  - curious
  - methodical
  - helpful

# Default test behaviors
behaviors:
  # Charlie is typically a regular member
  creates_entities: false
  invites_members: false
  manages_permissions: false

# Vault credentials (for test scenarios)
vault:
  password: "test-pass-charlie"

# Color for reports
color: "#F59E0B"  # Amber
